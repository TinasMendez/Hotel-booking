# Copy this file to .env at the repository root and adjust values
# Secrets intentionally use placeholder values – replace them before running locally or deploying.

# ----------------------- Database -----------------------
DB_ROOT_PASSWORD=changeme
DB_NAME=reservasdb
DB_USERNAME=app_user
DB_PASSWORD=changeme
DB_URL=jdbc:mysql://localhost:3306/reservasdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# ----------------------- JWT ----------------------------
# Must be at least 32 ASCII chars for HS256
JWT_SECRET=please-change-this-64-character-secret-string-1234567890abcd

# ----------------------- Mail ---------------------------
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS=false
MAIL_FROM=reservas@digitalbooking.local
MAIL_SUPPORT=reservas@digitalbooking.local

# ----------------------- Frontend integration -----------
FRONTEND_BASE_URL=http://localhost:4173
SUPPORT_PHONE=
SUPPORT_WHATSAPP_URL=

# ----------------------- Uploads ------------------------
UPLOADS_MAX_FILES_PER_DIR=500
UPLOADS_MAX_FILE_SIZE=5242880

# ----------------------- Frontend env vars --------------
VITE_API_BASE=http://localhost:8080/api
VITE_WHATSAPP_NUMBER=573000000000
VITE_WHATSAPP_MESSAGE=Hola! Me gustaría saber más sobre esta propiedad.
