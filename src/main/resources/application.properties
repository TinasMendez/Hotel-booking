spring.application.name=backend

# PUERTO DEL SERVIDOR
server.port=8080

# CONEXIÓN A BASE DE DATOS MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/reservasdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Tina050898
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# CONFIGURACIÓN DE JPA (hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS (permitir acceso desde React u otro frontend)
spring.web.resources.add-mappings=true

logging.level.root=DEBUG

logging.level.org.springframework.boot.web.embedded.tomcat=INFO

